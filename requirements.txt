# Minimal dependencies for the live demo (real STAC search).
gradio[mcp]
mcp[cli]>=1.2
pystac-client>=0.8
requests>=2.31
numpy>=1.24

# Optional — real inference & higher-resolution / Planetary Computer assets:
#   pip install rasterio numpy planetary-computer matplotlib
